\section{Introduction}

Finite-context world models often fail in a way that is easy to miss: they can
observe a decisive cue early and later produce a representation that behaves as
if the cue was never present.  This matters for physical intelligence because
action-relevant evidence is rarely synchronized with the moment of action.  A
robot can read a sign before turning away, see a door state before entering a
hallway, or observe an object identity before that object becomes occluded.  If
the interface to a frozen host exposes only a recent window, then locally
plausible prediction is not enough; the old evidence must survive long enough,
in a usable form, to change a later decision.

We study this failure as an \emph{interface} problem rather than a raw memory
capacity problem.  The question is not only whether a hidden state can memorize
something.  The stricter question is whether old visual evidence, after it has
left the legal context window, is (i)~\emph{retained} in a causal memory state,
(ii)~\emph{exposed} through a host-facing representation, and (iii)~\emph{usable}
by a bounded downstream controller.  These three claims are routinely conflated.
A model may store old evidence internally yet fail to expose it in its output.
A linear readout may decode a label through a shortcut if the current endpoint
still leaks the cue.  A native controller may succeed only because it received
the true goal rather than the memory-selected one.  Our evaluation is designed
to close each of these loopholes with matched controls.

We introduce a \emph{masked-evidence slot-memory interface}
(Figure~\ref{fig:architecture}).  Training is self-supervised: it never receives
cue labels, cue crops, or manually selected key frames.  A target miner divides
the causal render stream into temporal bins, scores visual saliency, selects
salient patch sets, and removes them from the visible stream.  A compact slot
transformer must then predict the stop-gradient latent targets of those hidden
patches from a small persistent memory state.  Cue labels enter only
\emph{after} training, when a post-hoc ridge readout tests whether the learned
memory exposes the old cue under matched full, reset, and no-state conditions.

The central empirical point is structural rather than merely numerical.  Under
the same caches, target mining, patch target encoder, loss, optimizer, and
readout, replacing the slot writer with GRU, LSTM, or Mamba-lite recurrence
weakens long-delay exposure.  The recurrent baselines often solve short,
waypoint-like rows, which is useful evidence that the task is not impossible;
their failure appears when the cue is old or distributed across object-like
visual evidence.  Slots help because the target itself is a \emph{set} of visual
facts, and a memory set is a better carrier for that target than a single
recurrent vector.

Concretely, on twelve OGBench render decks across a sweep of controlled
evidence delays and three seeds, the slot writer passes every registered
retention cell (36/36 environment-age rows, 108/108 seed cells) with mean
full-memory balanced accuracy $0.983$ while reset and no-state controls stay at
$0.250$ and $0.248$.  The strongest recurrent baseline reaches $0.803$ mean full
accuracy and $22/36$ rows, and its margin behind the slot writer widens as
evidence ages.  Where audited fixed controllers exist, memory-selected targets
execute at $0.957$ mean success across $27/36$ rows.

\paragraph{Contributions.}
\begin{itemize}
  \setlength\itemsep{1pt}
  \item A \textbf{fail-closed audit} for old-evidence memory in frozen
    world-model interfaces, separating retention, host exposure, and bounded
    use with matched reset / no-state controls.
  \item A \textbf{label-free masked patch-set JEPA objective} that trains a slot
    memory writer from automatically mined salient patches, with full pseudocode
    (Algorithms~\ref{alg:train}--\ref{alg:audit}).
  \item A \textbf{controlled architecture comparison} across twelve OGBench
    decks, three evidence ages, and three seeds, isolating the writer format
    while holding every other component fixed, extended with capacity-matched
    and structured-memory baselines (SlotSSM-style, gated slot attention,
    top-$k$ retrieval, cached attention).
  \item A \textbf{bounded streaming formulation} $M_t=f_\theta(M_{t-1},
    Z_{t-K+1:t})$ with token eviction and constant per-step memory/compute, plus
    a \textbf{delay-generalization} and \textbf{age-scaling} study to age~128.
  \item A \textbf{frozen-LeWM integration} showing the same interface exposes
    old evidence in the host's own output beyond its context window, and a
    \textbf{shortcut-broken benchmark} confirming retention of object identity
    rather than colour.
\end{itemize}

\noindent
Our contribution is best read as a \emph{fail-closed evaluation protocol plus a
controlled demonstration that target--carrier structural alignment improves
host-visible old-evidence exposure}, rather than a wholly new slot-memory
architecture: slot memory \citep{r_slotssm,r_gsa,r_slotmemory}, masked JEPA
prediction \citep{r_causaljepa}, and memory-augmented world models
\citep{r_onmemory,r_lewm} each exist; the novelty is the interface audit that
separates storage, host exposure, and use and shows where structural alignment
matters.

\section{Related Work}

\paragraph{World models under memory pressure.}
Latent world models compress observations into predictive state for control
\citep{r_dreamer}.  Recall-to-Imagine makes long-term memory and credit
assignment explicit bottlenecks for model-based RL \citep{r_r2i}.  Diffusion and
video world models face a related pressure: short observation windows can
produce plausible futures that drift from earlier evidence.  LoopNav turns this
into looped spatial-consistency tests \citep{r_loopnav}; StateSpaceDiffuser,
long-context state-space video models, and memory-expert diffusion world models
add state-space or expert memory paths to preserve temporal consistency across
long rollouts \citep{r_statespacediffuser,r_lcsvwm,r_memory_experts}.  We ask a
narrower but stricter interface question: not whether a generator looks
temporally consistent, but whether old evidence that is illegal for the current
context is still exposed in a frozen host-facing representation.

\paragraph{Partial observability and memory in RL.}
Partially observable RL has long treated memory as a substitute for unavailable
Markov state.  PO-Dreamer adds memory guidance to world models under partial
observability \citep{r_po_dreamer}, and memory traces replace fixed observation
windows with compact, analyzable history summaries \citep{r_memory_traces}.
These works optimize agents or value estimates.  In contrast, our audit isolates
a representation-interface claim: the downstream readout receives the same
endpoint under full, reset, and no-state conditions, so success must come from
retained old evidence rather than a policy learning to exploit a different
observation stream.

\paragraph{Sequence memory and learned state.}
GRU and LSTM recurrence remain standard baselines for compressing sequence
history \citep{r_gru,r_lstm}.  Mamba and Mamba-2 scale selective state-space
recurrence and connect it to attention-like computation
\citep{r_mamba,r_mamba2}.  Infini-attention, test-time-training layers, and
Titans push the fixed-context problem from compressive attention, self-supervised
hidden-state updates, and neural long-term memory
\citep{r_infini_attention,r_ttt,r_titans}.  SlotSSM and Gated Slot Attention are
closest to our writer because they make sequence memory modular or slot-like
rather than a single monolithic vector \citep{r_slotssm,r_gsa}.  These works
support the broad argument that fixed-window attention is insufficient for
long-horizon evidence; they do not, by themselves, answer whether a memory state
exposes the task-relevant old cue under leakage controls, which is exactly what
we hold fixed and test.

\paragraph{Structured and embodied memory.}
Object-centric world models show that decision-relevant visual details are often
small, structured, and easily washed out by reconstruction losses
\citep{r_oc_storm}.  SOLD and language-guided object-centric world models show
that slot latents can serve as relational state interfaces for manipulation
\citep{r_sold,r_object_wm_lgm}.  Embodied-memory systems such as KARMA,
Retrieval-Augmented Embodied Agents, Memo, and 3DLLM-Mem separate short-term
working context from longer experience or spatial-temporal memory
\citep{r_karma,r_raea,r_memo,r_3dllm_mem}; agent-memory work such as Memory-R1
and ReMemR1 studies what to store, update, and retrieve
\citep{r_memory_r1,r_rememr1}.  Evaluation work such as RULER and LongMemEval
warns that easy retrieval tests overstate real memory ability
\citep{r_ruler,r_longmemeval}.  Our slot memory is aligned with this structured
direction, but the supervision and claim differ: we train from label-free masked
visual patch sets and reveal labels only to audit whether stored evidence is
linearly accessible and physically useful.

\section{Problem Formulation}

Let $x_{0:T}$ denote rendered frames and $a_{0:T-1}$ actions.  A cue $c \in
\{0,1,2,3\}$ is visible at an early time and determines a later target label
$y=c$.  At readout time $t$, the frozen host sees only a recent context window
of length $K$; the cue lies strictly outside that window.  We call the delay
between the cue's last visible frame and the readout boundary the
\emph{evidence age} $\alpha$.  Rather than committing to a few fixed delays, we
treat $\alpha$ as an ordered stress variable evaluated on increasingly dense and
geometric grids; the values $\{4,8,15\}$ are simply convenient reference points
on that axis (protocol~P0), the dense-interpolation study (protocol~P1) fills in
$\{4,6,8,10,12,15,18\}$, and the long-horizon study
(Section~\ref{sec:age-scaling}, protocol~P2) extends $\alpha$ geometrically up to
$128$.  We report retention as a function of $\alpha$ rather than as pass/fail at
a hand-picked age.

\paragraph{Audit conditions.}
The audit has three conditions that share the same held-out episodes, endpoint,
and readout, differing only in the memory state fed to the readout.
The \emph{full} condition uses the causal memory state $M_t$ produced from the
full legal stream.  The \emph{reset} condition observes the same endpoint and
recent context but clears persistent memory at the context boundary.  The
\emph{no-state} condition removes persistent memory entirely.  Because all three
share the endpoint, any information recoverable from current pixels, the action
trace, class imbalance, or a forgiving readout raises \emph{all} conditions
together and therefore fails the audit.

\paragraph{Claim ladder.}
We report a four-rung ladder in which each rung is interpreted only after the
previous one passes.  \textbf{(1)~Demand:} the decisive cue must sit outside the
legal context window across the evaluated delay sweep.  \textbf{(2)~Retention:} full memory must
expose the old cue on held-out episodes.  \textbf{(3)~Controls:} reset and
no-state features must stay near chance.  \textbf{(4)~Use:} executed success is
reported only where a fixed local controller exists; missing rows are marked
controller-unavailable rather than counted as successes.  Formally, a seed cell
passes when
\begin{equation}
\mathrm{BAcc}_{\text{full}} \ge 0.750
\quad\wedge\quad
\mathrm{BAcc}_{\text{reset}} \le 0.350
\quad\wedge\quad
\mathrm{BAcc}_{\text{no-state}} \le 0.350,
\label{eq:gate}
\end{equation}
and an environment-age row passes only when all three seeds pass.  Chance
balanced accuracy is $0.25$ for the four-way cue.

\section{Method}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_c_architecture.pdf}
\caption{Masked-evidence slot-memory interface and its two data paths.  A causal
render stream is tokenized and written into eight memory slots $M_t$
(stages~1--3).  A label-free target miner withholds salient patch sets and
encodes them with a fixed encoder $\phi$ (stage~4); the slot writer predicts
those hidden targets through a set-prediction loss (stage~5, yellow gradient
path).  Cue labels are never used in training; a post-hoc ridge readout audits
$M_t$ under matched full / reset / no-state conditions (stage~6, dashed
eval-only path).}
\label{fig:architecture}
\end{figure}

Figure~\ref{fig:architecture} is the compact method contract.  A trajectory
cache fixes frames, actions, time indices, and train/validation splits for every
method so that only the memory writer changes.  We describe each component and
then give training and audit pseudocode.

\subsection{Inputs and tokenization}
For each episode the model receives the legal stream: rendered frames
$x_{0:t}$, actions $a_{0:t-1}$, and integer time indices.  A frame CNN maps each
frame to a token $z_i\in\mathbb{R}^{D}$ with $D=160$; learned action and time
embeddings are added, giving a token sequence $Z=[z_0,\dots,z_t]\in
\mathbb{R}^{t\times D}$.  The cue frames are inside the mined-and-hidden set at
training time and outside the length-$K$ window at audit time, so the model
never has a trivially legal copy of the answer.

\subsection{Masked patch-set target}
The training signal is old, visual, and label-free.  For each episode the target
miner splits the causal history $[0,t]$ into $V{=}3$ temporal bins.  Within each
bin it scores patch saliency (local color contrast and novelty against a running
mean), samples one salient view per bin proportionally to saliency, and extracts
a set of high-saliency patches.  The selected patches are \emph{removed from the
visible stream} and encoded by a fixed target encoder $\phi$ into pooled
color/texture latents,
\begin{equation}
Y \;=\; \{\,y_j\,\}_{j=1}^{N}, \qquad
y_j \;=\; \mathrm{stopgrad}\!\big(\phi(\text{patch}_j)\big) \in \mathbb{R}^{D},
\label{eq:target}
\end{equation}
where $\phi$ applies a fixed $4{\times}4$ adaptive pool, mean-centering, and a
saturation channel followed by a frozen random projection.  Because $\phi$ is
fixed and generic, the miner cannot smuggle a semantic label into the target: it
only sees color and texture structure.  Crucially, the target is a \emph{set},
which keeps object-level, spatially distributed structure intact rather than
collapsing it into a single frame reconstruction.

\subsection{Slot writer}
The writer maintains $S{=}8$ normalized memory slots.  Slots act as queries in a
compact multi-head cross-attention over the token sequence, followed by
LayerNorm and an MLP update; stacking these blocks yields the causal memory
state $M_t=[m_1,\dots,m_S]\in\mathbb{R}^{S\times D}$.  Writing one block is
\begin{align}
\tilde{M} &= M + \mathrm{MHA}\big(Q{=}W_q M,\; K{=}W_k Z,\; V{=}W_v Z\big),\\
M' &= \tilde{M} + \mathrm{MLP}\big(\mathrm{LN}(\tilde{M})\big),
\label{eq:slot}
\end{align}
with $H{=}4$ heads.

\subsection{Bounded streaming memory}
\label{sec:streaming}
A causal transformer over the full prefix is causal but not bounded: it can
re-read every historical token at readout.  To make the memory a genuine
bounded persistent state we use a chunked recurrence.  The legal stream is
ingested in consecutive chunks of at most $K$ tokens, and the slots attend only
over the previous slot state concatenated with the current chunk; the chunk
tokens are then \emph{evicted}:
\begin{equation}
M_t \;=\; f_\theta\big(M_{t-1},\; Z_{t-K+1:t}\big),
\qquad
\text{keys/values} = [\,M_{t-1}\,;\,Z_{t-K+1:t}\,],
\label{eq:streaming}
\end{equation}
so all tokens before $t-K+1$ are physically deleted and inaccessible.  Peak
activation memory and per-step compute are therefore constant in sequence
length (Section~\ref{sec:streaming-results}); with $K$ larger than the sequence
the update reduces exactly to the one-shot writer in Eq.~\eqref{eq:slot}, which
we keep as an ablation.  Because both the memory and the target are sets, the
model is not forced to squeeze every old fact into one recurrent vector or one
hand-indexed slot; different slots can specialize to different mined views.

\subsection{Set-prediction objective}
A prediction head $p$ maps slots to predicted target latents
$\hat{Y}=p(M_t)$.  Predictions are matched to the mined target set $Y$ with a
set loss that combines contrastive, cosine, and anti-collapse terms:
\begin{equation}
\mathcal{L}
= \underbrace{\mathcal{L}_{\text{InfoNCE}}(\hat{Y},Y)}_{\text{retain the right facts}}
+\; \lambda_{\cos}\, \underbrace{\big(1-\overline{\cos}(\hat{Y},Y)\big)}_{\text{align latents}}
+\; \lambda_{\text{std}}\, \underbrace{\mathrm{std\text{-}reg}(\hat{Y})}_{\text{anti-collapse}} .
\label{eq:loss}
\end{equation}
The InfoNCE term uses in-batch negatives so the memory must keep the correct old
view separable from other episodes' views; the cosine term aligns matched
latents; the standard-deviation regularizer prevents slot collapse.  No term
uses the cue label.

\subsection{Baselines under a shared contract}
GRU, LSTM, and a plain PyTorch Mamba-lite recurrence replace only the slot
writer.  They consume the identical token sequence and emit a state that is
passed through the same prediction head, same target miner, same encoder $\phi$,
same loss~\eqref{eq:loss}, and same readout.  This is the key experimental
control: any difference in exposure is attributable to the memory format, not to
the data, target, or optimizer.

\subsection{Post-hoc audit readout}
After training, a ridge classifier is fit on \emph{train} memory features and
cue labels, then applied unchanged to held-out full, reset, and no-state
features.  Training is thus deliberately asymmetric: it only applies
reconstruction pressure on hidden patch sets, while evaluation asks a semantic
question that was never optimized directly.  If labels entered training, a
reviewer could attribute success to supervised cue classification; if labels
were never used at all, there would be no way to audit whether the memory
retained the task-relevant cue.  The split between label-free training and
label-revealed evaluation is a measurement design, not a loophole.

\subsection{Retention timeline}
\label{sec:timeline}
It is worth stating the chronology explicitly, because the target miner and the
writer interact with the cue frames at different moments:
\begin{center}\small
cue frame observed $\;\to\;$ written to memory $\;\to\;$ cue tokens evicted
$\;\to\;$ distractor chunks processed $\;\to\;$ audit at readout.
\end{center}
The writer \emph{observes} the cue frames when they arrive and folds them into
the slot state; only \emph{afterwards} are those tokens evicted (streaming
writer) or pushed outside the legal window (one-shot writer), and the audit
happens later still, once several distractor chunks have been processed.  The
target miner masks salient \emph{patches} from the visible stream so the writer
cannot copy a legal answer, but this masking removes patches from the
\emph{prediction target}; the writer has already observed the cue frames before
they are hidden.  Retention is therefore genuine --- the memory carries evidence
it saw and then lost access to, rather than reading a cue that was never
deleted.

\begin{algorithm}[t]
\SetAlgoLined
\DontPrintSemicolon
\KwIn{legal stream $(x_{0:t},a_{0:t-1})$; fixed encoder $\phi$; slot writer $f_\theta$; head $p_\theta$}
\For{each training step}{
  $Z \leftarrow \mathrm{Tokenize}(x_{0:t}, a_{0:t-1}, \tau)$\;
  bins $\leftarrow \mathrm{SplitTemporal}(0{:}t, V{=}3)$\;
  \ForEach{bin $b$}{
    $v_b \leftarrow \mathrm{SampleSalientView}(b)$; \quad patches$_b \leftarrow \mathrm{TopSalientPatches}(v_b)$\;
  }
  $Y \leftarrow \{\,\mathrm{stopgrad}(\phi(\text{patch}))\,\}$;\quad remove mined views from visible $Z$\;
  $M \leftarrow M_0$ \tcp*{learned initial slots, $S$ slots}
  \ForEach{chunk $Z_j$ of $Z$ in chronological order}{
    $M \leftarrow f_\theta(M,\, Z_j)$ \tcp*{Eq.~\eqref{eq:streaming}; keys/values $=[M;Z_j]$}
    permanently delete $Z_j$ \tcp*{pre-chunk tokens are inaccessible}
  }
  $\hat{Y} \leftarrow p_\theta(M)$; \quad $\mathcal{L} \leftarrow$ Eq.~\eqref{eq:loss} \tcp*{no cue labels}
  update $\theta$ with AdamW on $\nabla_\theta \mathcal{L}$\;
}
\caption{Label-free masked patch-set training with the bounded streaming writer
  (per method; only $f_\theta$ changes).  With a chunk larger than the sequence
  the loop reduces to the one-shot writer $M\leftarrow f_\theta(Z)$ of
  Eq.~\eqref{eq:slot}, which we keep as an ablation.}
\label{alg:train}
\end{algorithm}

\begin{algorithm}[t]
\SetAlgoLined
\DontPrintSemicolon
\KwIn{trained writer $f_\theta$; held-out episodes; cue labels $y$ (eval only)}
$g \leftarrow \mathrm{RidgeFit}(\{f_\theta(Z^{\text{train}})\}, y^{\text{train}})$\;
\ForEach{condition $\in\{$full, reset, no-state$\}$}{
  $M^{\text{cond}} \leftarrow$ memory under condition (clear at $K$ / drop state)\;
  $\mathrm{BAcc}_{\text{cond}} \leftarrow \mathrm{BalancedAcc}(g(M^{\text{cond}}), y^{\text{val}})$\;
}
cell passes $\Leftarrow$ Eq.~\eqref{eq:gate}\;
\caption{Fail-closed audit (labels used only here).}
\label{alg:audit}
\end{algorithm}

\section{Experiments}
\label{sec:experiments-main}

\paragraph{Protocol.}
We evaluate twelve rendered OGBench decks \citep{r_ogbench}: PointMaze medium,
large, giant, and teleport; AntMaze large and giant; HumanoidMaze large; Cube
single, double, and triple; Puzzle-3x3; and Scene.  Every memory writer in the
main audit is run at ages $4/8/15$ and seeds $0/1/2$ ($108$ cells per method).
Figures and tables are generated from local JSON/NPZ artifacts by
\path{scripts/plot_paper_c.py}; per-environment numbers appear in
Appendix~\ref{app:full}.  Balanced accuracy is reported throughout; chance is
$0.25$.

\paragraph{Protocols.}
The paper reports results under four named protocols that differ in the writer,
the environment subset, and the evaluation grid; every results table and figure
caption states which protocol it uses (Table~\ref{tab:protocols}).  Protocol~P0
is the main controlled-age audit with the one-shot bounded-output writer; P1
tests dense delay interpolation; P2 tests long-horizon extrapolation with the
bounded streaming writer; and P3 is the capacity-matched, structured-deck
comparison at strictly bounded context.  Keeping the protocols explicit prevents
conflating numbers produced under different writers or environment subsets ---
in particular, the headline audit numbers come from P0 (one-shot), not from the
streaming writer.

\begin{table}[t]
\centering
\footnotesize
\caption{Named evaluation protocols.  \emph{Writer} is one-shot (full-prefix,
bounded output) or streaming (chunk eviction, bounded context).}
\label{tab:protocols}
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\linewidth}{llllX}
\toprule
Protocol & Writer & Train / eval ages & Envs & Config / source\\
\midrule
P0 (main audit) & one-shot & $\{4,8,15\}$ / $\{4,8,15\}$ & 12 decks &
  cue color, 3 seeds, $D{=}256$/$S{=}24$/96ep
  (\path{multiview_patchset_color_jepa_native_v1})\\
P1 (dense interp.) & one-shot & $\{4,8,15\}$ / $\{4,6,8,10,12,15,18\}$ & 12 &
  mixed-age training (\path{multiview_patchset_auto_age_v1})\\
Pc (continuous, primary) & one-shot & $[4,136]$ / $[4,136]$ log-unif.\ (per ep.) & 4 &
  continuous train+eval delays (\path{paper_c_continuous_v1})\\
P2 (long-horizon) & streaming & $\{4,8,16\}$ / $\{4,8,16,32,64,128\}$ & 4 &
  re-rendered episodes (\path{paper_c_agescale_v1})\\
P3 (capacity/struct.) & streaming & $15$ / $15$ & 3 struct. &
  cube-single, puzzle-3x3, scene; $D{=}160$/$S{=}8$/36ep, $K{=}4$
  (\path{paper_c_revision_v1/stream_age15_s0})\\
\bottomrule
\end{tabularx}
\end{table}

\paragraph{Reconciling the age-15 numbers.}
Because different protocols use different writers, environment subsets, and
capacities, several distinct ``age-15 slot'' numbers appear in the paper.
Table~\ref{tab:headline-recon} lists each with its exact settings so they are
not confused.  The headline $0.962$ is the P0 one-shot writer averaged over all
twelve decks; the lower structured-deck numbers reflect harder decks, matched
capacity, and (for two of them) strictly bounded streaming context.

\begin{table}[t]
\centering
\footnotesize
\caption{Every reported age-15 slot balanced accuracy with its protocol and
settings.  ``One-shot'' re-reads the full prefix (bounded output); ``streaming''
evicts chunks (bounded context, chunk $K{=}4$).  Values are read from the source
artifacts of each protocol.}
\label{tab:headline-recon}
\setlength{\tabcolsep}{4pt}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lllllllll}
\toprule
Age-15 BAcc & Protocol & Env subset & Cue & Train ages & Eval age & Writer & Chunk & $D$/$S$ (seeds)\\
\midrule
\rowcolor{MemCream}
$0.962$ & P0 & 12 decks & color & $4/8/15$ & 15 & one-shot & none & $256$/$24$ (3)\\
$0.735$ & P3 (cap.-matched) & 3 struct.\ decks & color & 15 & 15 & streaming & 4 & $160$/$8$ (1)\\
$0.818$ & P3 (ablation) & cube-single, puzzle-3x3 & color & 15 & 15 & one-shot & none & $160$/$8$ (1)\\
$0.671$ & P3 (ablation) & cube-single, puzzle-3x3 & color & 15 & 15 & streaming & 4 & $160$/$8$ (1)\\
\bottomrule
\end{tabular}}
\end{table}

\subsection{Architecture comparison under a shared contract}
Table~\ref{tab:main-comparison} and Figure~\ref{fig:baseline-comparison} give
the headline comparison.  The pattern, not any single number, is the claim: full
memory is high while reset and no-state stay near chance, and the recurrent
baselines that solve many short or easy rows lose the exposure pattern on older
or more structured evidence.  Controls are matched at the feature level, so a
deck that leaked the cue through endpoint pixels, actions, class imbalance, or a
forgiving readout would raise the controls and fail.  The slot writer passes all
$36$ environment-age rows and all $108$ seed cells; the strongest recurrent
baseline (GRU) passes $22$ rows and $73$ cells, and LSTM collapses to $6$ rows.

\begin{table}[t]
\centering
\footnotesize
\caption{Controlled-age retention and exposure over 12 decks, 3 ages, and 3
seeds (protocol~P0, one-shot writer).  Full, reset, and no-state are balanced
accuracies from the shared post-hoc readout; controls are matched on the same
held-out rows.}
\label{tab:main-comparison}
\input{generated_results/main_comparison_tabular.tex}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.86\linewidth]{fig_c_baseline_comparison.pdf}
\caption{Architecture comparison with all non-memory components held fixed
(protocol~P0).  The slot writer dominates mean retention, the age-15 stress
test, and the fail-closed all-pass count.}
\label{fig:baseline-comparison}
\end{figure}

\subsection{Shortcut diagnostics}
Figure~\ref{fig:cue-inference} makes the control behavior explicit on the
hardest deck (Puzzle age~15).  In the cue-inference panels (a), full memory
concentrates readout evidence on the true old cue, while reset and no-state are
not cleanly random: they collapse toward endpoint-biased columns, which is
exactly the shortcut the audit is designed to expose.  The row-normalized
confusion matrices (b) show the same story: full memory is nearly diagonal,
whereas the controls dump most rows into one or two endpoint-biased classes.
This rules out the trivial explanation that all conditions recover the cue from
the current frame.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_c_cue_inference.pdf}
\caption{Shortcut diagnostics on held-out Puzzle age-15 features (protocol~P0).
(a)~Cue inference: dashed boxes mark the true cue, solid boxes the prediction;
full memory tracks the old cue while the controls collapse toward endpoint bias.
(b)~Row-normalized confusion: full memory is diagonal, the controls are not.}
\label{fig:cue-inference}
\end{figure}

\subsection{Evidence-age stress and unseen-age generalization}
We treat evidence age as a continuous stress variable rather than a set of
fixed checkpoints (Table~\ref{tab:delay-summary}, Figure~\ref{fig:age-gen}a).
The useful signal is the shape of the retention curve as the delay grows: LSTM
decays first; GRU and Mamba-lite stay stronger but fail once evidence is old;
the slot writer holds a stable exposure margin across the sweep.  The per-writer
summary makes the ordering explicit --- the slot writer keeps the highest mean
full-memory exposure and the largest passing delay while both controls stay at
chance --- and the separation grows monotonically with delay rather than being
an artifact of one chosen age (the dense-grid delay curves of protocols~P1--P2
in Section~\ref{sec:revision} make this explicit).

We further ask whether the interface is tied to the specific training delays.
We train the slot writer on a \emph{mixture} of delays and evaluate on a denser
held-out grid that interpolates between and beyond them (protocol~P1), so that
the intermediate delays are never seen during training.
Figure~\ref{fig:age-gen}b shows that every held-out delay clears the same
fail-closed gate, with full-memory exposure indistinguishable from the trained
delays and both controls at chance.  The delay at which evidence must be
retained is therefore not memorized as a fixed offset; mixed-age training yields
a delay-robust interface.  We are careful about the boundary: this demonstrates
unseen-delay \emph{generalization}, not a learned explicit age router or an
automatic maximum-age search, which we leave to future work
(Appendix~\ref{app:autoage}).

\begin{table}[t]
\centering
\footnotesize
\caption{General delay-sweep summary per memory writer (no per-age columns).
For the slot writer, means are aggregated over the dense mixed-age delay sweep
(protocol~P1: all evaluated delays and seeds); for the recurrent baselines they
are over the evaluated delays (protocol~P0).  ``Max.\ passing delay'' is the
largest evaluated delay whose mean full-memory balanced accuracy clears the
$0.750$ gate (evaluated ranges differ by protocol; the long-horizon extension is
in Table~\ref{tab:max-delay}).}
\label{tab:delay-summary}
\input{generated_results/delay_summary_tabular.tex}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_c_age_generalization.pdf}
\caption{(a)~Fixed-age training (protocol~P0): recurrent baselines decay as
evidence ages, while the slot writer stays above the gate.  (b)~Mixed-age
training (protocol~P1) generalizes to unseen ages (diamonds); full-memory
exposure stays high while reset and no-state controls remain at chance.}
\label{fig:age-gen}
\end{figure}

\subsection{Retention as a continuous function of delay}
\label{sec:continuous-retention}
Our primary retention result treats the evidence delay as a genuinely
continuous variable rather than a set of fixed checkpoints or even a denser
discrete grid.  We train a single slot writer with the cue delay sampled
\emph{per episode} from a log-uniform distribution over the legal range
$[4,\alpha_{\max}]$, where $\alpha_{\max}$ is the largest delay the cache
supports ($136$ on the $140$-frame long-horizon caches), and then evaluate each
held-out episode at an independent delay drawn from the same continuous
distribution.  Figure~\ref{fig:continuous-retention} plots balanced accuracy
against $\alpha$ on a logarithmic axis for the four re-rendered long-horizon
environments, overlaying the binned empirical accuracy, a smoothed retention
curve, the reset control, and the $0.75$ gate.  Because every episode uses its
own delay, the curve is a dense sweep over the whole range instead of a handful
of enumerated ages.

We summarize each environment with three general, age-agnostic metrics rather
than a per-age table (Table~\ref{tab:retention-summary}): the \emph{empirical
maximum passing delay} (the largest $\alpha$ at which the smoothed curve still
clears the $0.75$ gate), the \emph{retention AUC} (the smoothed balanced
accuracy integrated over $\log\alpha$ and normalized to $[0,1]$), and the
balanced accuracy at the maximum tested delay.  These compact metrics describe
retention as a property of the entire delay axis, not of any single hand-picked
age, while the reset control stays at chance throughout.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_c_continuous_retention.pdf}
\caption{Primary retention result: balanced accuracy versus evidence delay
$\alpha$ (log axis) under a continuous log-uniform delay sweep, with both the
training and evaluation delays drawn per episode over the legal range
(\protect\path{outputs/paper_c_continuous_v1}).  Points are binned empirical
balanced accuracy, the black curve is the smoothed retention estimate, the grey
curve is the reset control, the dashed line is the $0.75$ gate, and the dotted
vertical line marks the empirical maximum passing delay.}
\label{fig:continuous-retention}
\end{figure}

\begin{table}[t]
\centering
\footnotesize
\caption{General per-environment retention summary from the continuous delay
sweep (no per-age columns): empirical maximum passing delay (smoothed $0.75$
crossing), retention AUC over $\log\alpha$ (normalized to $[0,1]$), and balanced
accuracy at the maximum tested delay (\protect\path{outputs/paper_c_continuous_v1}).}
\label{tab:retention-summary}
\input{generated_results/retention_summary_tabular.tex}
\end{table}

\paragraph{Per-environment delay curves.}
Figure~\ref{fig:env-heatmaps} complements the continuous sweep with the
mixed-age model evaluated across the delay range for all twelve decks.
Panel~(a) shows full-memory balanced accuracy staying well above the gate as
the delay grows for navigation decks, with a gentler curve on the structured
manipulation and scene decks where object identity, order, or configuration
must be preserved.  Panel~(b) plots the audit margin (full minus the stronger
control) against delay: it stays comfortably positive across the suite, so the
exposure is not carried by any single environment or any single chosen delay.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_c_env_heatmaps.pdf}
\caption{Per-environment retention versus evidence delay (protocol~P1; the
mixed-age model evaluated across the delay range rather than at a fixed-age
grid).  (a)~Full-memory balanced accuracy decays smoothly with
delay, most gently on navigation and most steeply on structured manipulation
decks.  (b)~Audit margin (full $-\max(\text{reset},\text{no-state})$) stays
positive across environments and delays.}
\label{fig:env-heatmaps}
\end{figure}

\subsection{Bounded native use}
The native-use stage asks a different question from the readout audit
(Figure~\ref{fig:native-use}, Table~\ref{tab:native-use}).  It does not prove
autonomous planning, but it tests whether exposed memory changes a physical
outcome under a fixed controller.  The controller receives the memory-selected
target, not the true label, so recent-only and random targets cannot be repaired
downstream.  Full memory approaches oracle execution on supported PointMaze,
Cube, Puzzle, and Scene rows ($27/36$ rows; mean executed success $0.957$; worst
scored row $0.828$), a mean lift of about $0.68$ over both the recent-only and
random-target controls.  AntMaze and HumanoidMaze are outside the native-use
claim because the repository does not yet contain audited local low-level
controllers for those bodies; those rows are reported as controller-unavailable
in Appendix~\ref{app:full}, not as failures.  We treat this stage as a
\emph{bounded-use sanity check}: it shows that a correctly exposed memory label
selects a target a fixed controller can execute, not that the memory performs
world-model planning.  The stronger world-model evidence is the frozen-LeWM
integration in Section~\ref{sec:revision}, where the memory changes the host's
own predictive output.

\begin{figure}[t]
\centering
\includegraphics[width=0.78\linewidth]{fig_c_native_heatmap.pdf}
\caption{Fixed-controller native use on supported decks (protocol~P0).  Each cell
shows full-memory executed success and, below it, the gain over the stronger
recent-only or random-target control.}
\label{fig:native-use}
\end{figure}

\begin{table}[t]
\centering
\footnotesize
\caption{Fixed-controller native-use summary for supported OGBench rows
(protocol~P0).}
\label{tab:native-use}
\input{generated_results/native_use_tabular.tex}
\end{table}

\section{Bounded memory, capacity, robustness, and world-model integration}
\label{sec:revision}

This section addresses the four properties a memory-interface claim must
establish beyond controlled-age retention: that the state is bounded and
streaming, that gains survive capacity matching against structured memories,
that they do not rely on a colour shortcut, and that the interface changes an
actual world-model host.

\subsection{The memory is bounded and streaming}
\label{sec:streaming-results}
We distinguish two notions of boundedness.  In \emph{all} experiments the slot
memory is bounded in \emph{output}: the persistent state the readout consumes is
a fixed $S\times D$ tensor regardless of sequence length.  The
\emph{streaming} writer additionally bounds the attention \emph{context} via the
chunked recurrence of Eq.~\eqref{eq:streaming}, evicting each chunk after it is
consumed so that peak activation memory and per-step compute are constant in
sequence length (Figure~\ref{fig:streaming-cost}).  The one-shot full-prefix
writer, by contrast, bounds only the output while re-reading every historical
token, so its peak activation memory grows with length.  To be explicit about
which writer produces which number: the main retention and exposure audit tables
in Section~\ref{sec:experiments-main} (protocol~P0) use the \emph{one-shot}
bounded-output writer, and the streaming variant attains slightly lower accuracy
at strictly bounded context (Table~\ref{tab:headline-recon}, and the
one-shot-vs-streaming ablation in Appendix~\ref{app:ablations}).  We do
\emph{not} claim the headline numbers are produced by the streaming writer; we
instead validate that the streaming writer retains old evidence broadly across
environments at bounded context (Table~\ref{tab:stream-headline}).

\begin{figure}[t]
\centering
\includegraphics[width=0.86\linewidth]{fig_c_streaming_cost.pdf}
\caption{Bounded streaming memory.  (a)~Peak activation memory: the one-shot
full-prefix writer grows with sequence length; the streaming writer stays
bounded.  (b)~Per-step compute is constant for the streaming writer.}
\label{fig:streaming-cost}
\end{figure}

Table~\ref{tab:stream-headline} reports the bounded streaming writer
($K{=}4$, chunk eviction) across the navigation and locomotion decks at the
registered ages.  Even at strictly constant per-step memory and compute, the
streaming writer clears the fail-closed gate on average at every age while the
reset and no-state controls stay at chance, so bounded-context retention is a
property that holds broadly, not only on a single deck.

\begin{table}[t]
\centering
\footnotesize
\caption{Bounded-streaming retention across environments (streaming writer,
$K{=}4$, chunk eviction; \protect\path{outputs/paper_c_revision_v1/stream_headline}).
Mean full/reset/no-state balanced accuracy over the completed
navigation/locomotion decks per age (the sweep is partial; the \emph{Envs}
column reports how many decks completed).}
\label{tab:stream-headline}
\input{generated_results/stream_headline_tabular.tex}
\end{table}

\subsection{Gains survive capacity matching and structured baselines}
Because the audit readout consumes a mean-pooled $D$-dimensional vector for
\emph{every} method, the comparison is already dimension-matched at the
readout.  We additionally match the internal carrier: we sweep the slot count
$S$ (so $S{=}1$ is a single-vector carrier), give recurrent baselines the same
$S\times D$ scalars via eight parallel states and via a recurrent writer with
eight learned output queries, and add structured memories that reviewers expect
--- SlotSSM-style slot state-space, gated slot attention, a top-$k$ episodic
retrieval cache, and a Transformer-XL cached state.
Figure~\ref{fig:capacity} plots age-15 full-memory accuracy against exposed
state scalars and compares all carriers under the shared contract (protocol~P3:
the three structured decks, streaming $K{=}4$, matched $D{=}160$).  The slot
interface remains at the top of the accuracy-vs-capacity frontier: matching the
carrier size does not close the gap, and structured recurrent/retrieval
memories do not match the slot writer's long-age exposure at equal state size.
We are careful not to over-claim here.  On these hardest decks the slot writer's
mean full-memory balanced accuracy is $0.735$
(Table~\ref{tab:structured}) --- it is the \emph{strongest} carrier under matched
capacity, comfortably ahead of the next-best structured memory (GSA at $0.669$)
and every recurrent baseline, but it sits just \emph{below} the $0.750$
registered gate of Eq.~\eqref{eq:gate}.  We therefore report P3 as a
capacity-matched \emph{ranking} of carriers on the hardest, bounded-context
setting, not as clearing the gate: the gate-clearing retention claim belongs to
P0 (Table~\ref{tab:main-comparison}), where the one-shot writer passes every
registered cell.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_c_capacity_structured.pdf}
\caption{Capacity-matched and structured comparison at age~15 (protocol~P3).
(a)~Full-memory
balanced accuracy across recurrent, state-space, slot, retrieval, and cached
baselines under the identical target/loss/readout contract.  (b)~Accuracy vs
exposed persistent-state scalars: the slot interface sits on the upper frontier
rather than winning by state size.}
\label{fig:capacity}
\end{figure}

\begin{table}[t]
\centering
\footnotesize
\caption{Structured and capacity-matched memory baselines (protocol~P3; age~15,
matched $D{=}160$, streaming $K{=}4$).  State scalars are the exposed
persistent-state size.  The slot writer is the strongest carrier at $0.735$ but
sits just below the $0.750$ gate on these hardest decks.}
\label{tab:structured}
\input{generated_results/structured_baselines_tabular.tex}
\end{table}

\subsection{Retention is not a colour shortcut}
The default cue makes the label recoverable from pooled colour, which the fixed
target encoder preserves.  We therefore add a shortcut-broken benchmark in
which the label is the cue \emph{shape identity} while its colour is randomized
independently of the label, so pooled colour is uninformative.
Figure~\ref{fig:shortcut} shows that the slot interface still exposes the old
identity well above the matched controls, while the controls stay at chance:
the memory retains object-like structure, not merely a hue.

\begin{figure}[t]
\centering
\includegraphics[width=0.52\linewidth]{fig_c_shortcut.pdf}
\caption{Randomized-colour identity cue (protocol~P3, structured decks): colour
is decorrelated from the label, so pooled colour cannot solve the task.  Full
memory still separates the old cue while reset and no-state remain at chance.}
\label{fig:shortcut}
\end{figure}

\subsection{Integration into a frozen LeWM world-model host}
To show the interface changes an actual world model rather than a standalone
probe, we inject the slot memory as a carrier into the frozen official LeWM
predictor \citep{r_lewm} on a PushT visual-binding task, where the decisive cue
lies outside the host's context window.  We measure whether the frozen host's
\emph{own output} exposes the old evidence under matched full/reset/no-state
controls, with cue labels used only for the post-hoc readout.
Figure~\ref{fig:lewm} shows that the injected memory raises host-output balanced
accuracy far above both controls on a six-way binding task at age~15 across five
seeds, clearing the same fail-closed gate.  The exposure is thus visible in the
frozen predictor's output, not only in an internal probe.

\begin{figure}[t]
\centering
\includegraphics[width=0.42\linewidth]{fig_c_lewm_host.pdf}
\caption{Frozen-LeWM integration.  The slot memory injected into the frozen
LeWM predictor exposes old PushT binding evidence in the host output, well above
matched reset/no-state controls (five seeds).}
\label{fig:lewm}
\end{figure}

\paragraph{Frozen-LeWM host-writer: specification.}
We inject a slot-memory residual into the \emph{frozen} official PushT LeWorldModel (\texttt{quentinll/lewm-pusht}, rev.\ \texttt{22b330c}; ViT-S encoder $+$ 6-layer causal predictor, 192-d latents, 3-frame context, action dim 10), keeping \emph{both} encoder and predictor frozen (state-dict digest asserted unchanged). A Host-Aligned Evidence Writer ($6$ slots, dim $256$, $4$ heads, $\sim$2.76M params) forms slots from the full prefix and adds a gated residual $\mathbf z^{\text{ctx}}\!\leftarrow\!\mathbf z^{\text{ctx}}+ s\cdot g\odot\Delta$ into the final legal context $\{16,17,18\}$. The adapter is trained \emph{label-free} (contrastive cue-delta targets, 64 epochs, AdamW $3\!\times\!10^{-4}$); semantic labels are used only for a post-hoc Ridge readout on the host's own $192$-d next-step output. The task is 6-way multi-item visual-binding recall (color-order permutations) with the cue placed $15$ steps before the decision; $1200$ train / $480$ val episodes, $5$ seeds.

\begin{table}[t]
\centering
\small
\begin{tabular}{lc}
\toprule
Condition & Host-output balanced acc.\ (mean $\pm$ s.d.) \\
\midrule
host-only (no memory) & $0.161 \pm 0.019$ \\
\textbf{correct memory} & $\mathbf{0.919 \pm 0.006}$ \\
reset prefix & $0.151 \pm 0.016$ \\
no-state & $0.161 \pm 0.019$ \\
shuffled episode & $0.168 \pm 0.013$ \\
random memory (matched norm) & $0.170 \pm 0.019$ \\
memory-only probe & $0.908 \pm 0.009$ \\
\midrule
chance ($1/6$) & $0.167$ \\
\bottomrule
\end{tabular}
\caption{Control battery for the frozen-LeWM host-writer (age 15, 5 seeds). Adapters and cue cache are reused from the primary run; no retraining.}
\label{tab:c-lewm-controls}
\end{table}

Injecting the correct-episode memory lets the frozen host expose the out-of-window cue in its \emph{own} output at $0.919$ balanced accuracy, whereas the native host, a reset prefix, a shuffled-episode memory, and a matched-norm random residual all sit at chance ($0.15$--$0.17$), showing the effect is episode-specific and not an injection-path artifact. The memory-only probe reaches $0.908$, so the evidence is already linearly decodable from the injected memory and the host reproduces essentially all of it, adding only a small transformation (Fig.~\ref{fig:c-lewm-controls}).

\begin{figure}[t]
\centering
\includegraphics[width=0.62\linewidth]{fig_c_lewm_controls.pdf}
\caption{Frozen-LeWM host-writer control battery (age~15, five seeds).  Only the
correct-episode memory and the memory-only probe expose the out-of-window cue in
the host's own output; host-only, reset, no-state, shuffled-episode, and
matched-norm random memory all sit at chance ($1/6$).}
\label{fig:c-lewm-controls}
\end{figure}

\subsection{Age scaling and empirical maximum passing delay}
\label{sec:age-scaling}
Finally we test how far back the interface retains evidence (protocol~P2).  We
re-render longer episodes for four environments (PointMaze-large, Cube-single,
Puzzle-3x3, and Scene), train on ages $\{4,8,16\}$ with the bounded streaming
writer, and evaluate on a geometric ladder up to age~128 at fixed per-step
budget (Figure~\ref{fig:age-scaling}).  Rather than fitting a decay constant, we
report the \emph{empirical maximum passing delay}: the largest evaluated age at
which full-memory balanced accuracy still clears the $0.750$ gate.
Table~\ref{tab:max-delay} lists this quantity and the balanced accuracy at
age~128 per environment.  Retention extends far beyond the registered ages on
the navigation and single-object decks (PointMaze-large and Puzzle-3x3 pass to
age~64, Cube-single all the way to age~128) and is shortest on the cluttered
Scene deck (age~16), while the reset control stays at chance throughout.  This
quantifies the maximum passing delay directly rather than asserting a single
stress point.

\begin{figure}[t]
\centering
\includegraphics[width=0.58\linewidth]{fig_c_age_scaling.pdf}
\caption{Long-delay scaling to age~128 with the bounded streaming writer
(protocol~P2), one curve per re-rendered environment at fixed per-step
memory/compute.  Full-memory retention decays smoothly while the reset control
stays at chance; the per-environment maximum passing delay is reported in
Table~\ref{tab:max-delay}.}
\label{fig:age-scaling}
\end{figure}

\begin{table}[t]
\centering
\footnotesize
\caption{Empirical maximum passing delay (largest evaluated age with full-memory
BAcc $\ge 0.750$) and full-memory BAcc at age~128, per re-rendered environment
(protocol~P2, bounded streaming writer; \protect\path{outputs/paper_c_agescale_v1}).}
\label{tab:max-delay}
\input{generated_results/max_delay_tabular.tex}
\end{table}

\subsection{Write-time attention correlates with the cue interval, and cue
frames are causally necessary}
\label{sec:discovery}
A final question is whether the label-free memory actually \emph{finds} the
decisive past observation or merely stores a diffuse summary.  We extract the
slot-to-frame cross-attention on held-out episodes and plot it over time
(Figure~\ref{fig:evidence}): it concentrates sharply on the cue-visible window
even though that window left the legal context long before readout, and correct
decisions place more attention mass there than incorrect ones.

The attention visualized in Fig.~\ref{fig:evidence} is one-shot \emph{read-time}
slot$\to$frame cross-attention from the full-prefix writer (chunk$=0$; a
strictly streaming writer would instead expose write-time attention). To test
whether this attention reflects causal attribution rather than mere
concentration, we delete input frame tokens \emph{before the writer reads them}
(setting \texttt{valid}$=0$ and zeroing the token) and recompute the frozen
ridge readout: deleting the three cue frames collapses balanced accuracy from
$\{1.00,0.94,1.00,0.97\}$ to near chance ($0.25$), a drop of
$\Delta=0.77/0.67/0.66/0.73$ on PointMaze/Puzzle/Cube/Scene ($\pm{\le}0.04$,
$2000$-sample episode bootstrap), establishing that cue frames are causally
necessary. Crucially, deleting the three \emph{most-attended} old
frames---selected by the model's read-time attention with no cue
knowledge---reduces accuracy far more than deleting three random or three recent
legal frames ($\Delta_{\text{top-3}}=0.53/0.10/0.65/0.16$ vs.\
$\Delta_{\text{random}}\le0.01$), so attention localizes causally load-bearing
evidence rather than merely correlating with it. The effect tracks
concentration: the cue-window attention mass exceeds the uniform baseline by
$2.91\times$ $[2.76,3.06]$ (PointMaze), $1.86\times$ $[1.82,1.89]$ (Cube), and
$1.41\times$ (Puzzle $[1.34,1.48]$, Scene $[1.36,1.46]$), correcting our earlier
imprecise ``$\sim$2--3$\times$'' claim; single-frame deletions barely move
accuracy ($\Delta\le0.06$) because the cue is shown redundantly across three
frames, which is why all interventions are count-matched at three deleted
frames.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_c_evidence_discovery.pdf}
\caption{Autonomous evidence discovery (one-shot read-time slot attention).
(a)~Slot attention mass over time: it peaks on the cue-visible window (yellow)
rather than the legal readout window (gray), and correct decisions attend it
more than incorrect ones.  (b)~Fraction of attention mass inside the cue window
versus a uniform baseline; the label-free memory discovers the decisive frames
well above chance.}
\label{fig:evidence}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig_c_causal_evidence.pdf}
\caption{Causal necessity of the cue frames.  Deleting the three cue frames
before the writer reads them collapses balanced accuracy to near chance, and
deleting the three most-attended old frames (selected by read-time attention,
with no cue knowledge) hurts far more than deleting random or recent legal
frames --- so read-time attention localizes causally load-bearing evidence.
Interventions are count-matched at three deleted frames; error bars are a
$2000$-sample episode bootstrap.}
\label{fig:causal-evidence}
\end{figure}

\section{Discussion}

The experimental picture is coherent.  The target miner creates an old,
label-free visual prediction problem; the slot writer provides a memory format
that matches the target's multi-part structure; the readout audit shows that old
evidence is exposed rather than hidden inside an inaccessible state; and the
fixed-controller stage shows that exposed evidence can alter execution when a
valid controller exists.  Three insights follow.

\paragraph{Structure-preserving targets matter more than another recurrent block.}
Flat whole-frame reconstruction can look like memory while remaining too diffuse
for object-level cues; once the target becomes a \emph{set} of salient patches,
the same slot bottleneck becomes trainable without cue labels.  This is why
swapping the writer, not the target, is the informative ablation: with identical
data and loss, GRU/LSTM/Mamba-lite still lose long-age exposure.

\paragraph{Short-horizon competence is not long-horizon exposure.}
GRU and Mamba-lite often clear easy short-delay rows, yet the long-delay curve
and the Puzzle/Scene/Cube gaps show where a single hidden vector stops keeping
separable visual facts.  Reporting only mean accuracy would hide this; the delay
sweep and the per-environment retention curves are what localize the failure.

\paragraph{Internal storage is not host exposure.}
The fail-closed controls enforce a distinction that a raw memory-capacity metric
misses: a carrier can score well on an internal probe while a frozen interface
still fails to expose old evidence as evidence age grows.  Our audit therefore
reports retention, exposure, and use as separate rungs rather than a single
pooled score.

\paragraph{Delay robustness is a property of the interface, not the offset.}
Mixed-age training generalizes to unseen delays
(Figure~\ref{fig:age-gen}b), so the exposure is not a
memorized offset.  The boundary is still explicit: this is unseen-age
generalization, not a learned age router that decides \emph{how far back} to
retrieve, nor an automatic maximum-age search; those remain future work
(Appendix~\ref{app:autoage}).  The clean claim is a controlled memory-interface
result: a masked-evidence slot interface retains and exposes old visual evidence
more reliably than recurrent alternatives trained with the same objective, it
generalizes across evidence delays, and the exposed state can drive
fixed-controller execution where controllers exist.

\section{Conclusion}

Old-evidence memory should be evaluated by retention, exposure, and bounded use.
A hidden state that stores information but never exposes it is not enough for a
frozen world-model interface; a readout that succeeds without controls is not
enough either.  The masked-evidence slot-memory interface satisfies this stricter
ladder on the registered controlled-age suite (protocol~P0) and separates
cleanly from GRU, LSTM, and Mamba-lite writers under a shared training and
evaluation contract.  The revision fills in the fuller story: the memory is
bounded in both output ($S\times D$ state in every experiment) and, in the
streaming writer, context (constant per-step memory and compute via chunk
eviction); dense-grid delay curves show the exposure is a smooth function of
evidence age rather than an artifact of a few hand-picked delays; a long-horizon study
quantifies the empirical maximum passing delay out to age~128; capacity-matched
and structured-memory baselines confirm the slot interface is the strongest
carrier at matched state size even where it sits just below the gate on the
hardest decks; a shortcut-broken identity task shows the memory retains
object-like structure rather than a colour; the frozen-LeWM host exposes the
old cue in its \emph{own} output with a full control battery; and a
counterfactual deletion test establishes that the cue frames are causally
necessary and that read-time attention localizes them.  The clean claim remains
a scoped, fail-closed evaluation protocol plus a controlled demonstration that
target--carrier--host structural alignment improves host-visible old-evidence
exposure.

\section{Limitations}

The benchmark is controlled by design.  Cues are synthetic overlays on rendered
OGBench streams, which enables shortcut audits but does not cover all real robot
memory patterns.  The native-use claim depends on fixed controllers and is
reported only for environments with audited local controllers.  The Mamba-lite
baseline tests selective recurrence but is not the optimized official Mamba
stack.  The readout uses labels after training, so this is an audit of exposed
representation quality rather than an end-to-end policy.

Several directions remain genuinely open.  (i)~\emph{Automatic retrieval
routing}: the current interface exposes a bounded memory but does not learn
\emph{which} stored slot to consult for a given query, so a content-,
timestamp-, and confidence-aware router is future work.  (ii)~\emph{Interference
between multiple old events}: our tasks isolate one decisive cue, and we do not
yet study how several old events compete inside a fixed-size slot set.
(iii)~\emph{Overwrite and recency competition}: with a bounded state and
eviction, understanding when new evidence should overwrite old evidence is
unresolved.  (iv)~\emph{Natural observations}: cues are synthetic, and moving to
non-synthetic robot observations is necessary to claim real-world memory.
(v)~\emph{End-to-end planning with memory}: we audit exposure and bounded use,
not closed-loop planning that consumes the memory.  Finally, our causal analysis
(Section~\ref{sec:discovery}) inspects one-shot \emph{read-time} attention;
analyzing the \emph{write-time} attention of a strictly streaming writer, which
would show \emph{when} evidence is committed rather than \emph{how} it is later
read, remains future work.
